/**
 * API Routes Index
 * All route files will be registered here and mounted in app.js
 *
 * Usage in app.js:
 *   const routes = require('./routes');
 *   app.use('/api', routes);
 */
const express = require('express');
const router = express.Router();

// Mount module routes here as they are built:
// router.use('/auth', require('../modules/auth/auth.routes'));
// router.use('/otp', require('../modules/otp/otp.routes'));
// router.use('/members', require('../modules/members/members.routes'));
// router.use('/admins', require('../modules/admins/admins.routes'));
// router.use('/dashboard', require('../modules/dashboard/dashboard.routes'));
// router.use('/exports', require('../modules/exports/exports.routes'));
// router.use('/notifications', require('../modules/notifications/notifications.routes'));
// router.use('/audit-logs', require('../modules/auditLogs/auditLogs.routes'));
// router.use('/contacts', require('../modules/contacts/contacts.routes'));
// router.use('/health', require('../modules/health/health.routes'));

module.exports = router;
